# Add strings on each line to be passed as "--tag" values to the `docker build` command.
#
# Example:
#
# ahawker/cookie-sheet:1.2.3

{{ cookiecutter.image_repo }}/{{ cookiecutter.image_name }}:{{ cookiecutter.version }}
{{ cookiecutter.image_repo }}/{{ cookiecutter.image_name }}:latest
